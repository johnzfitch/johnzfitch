# Ignore large/generated assets and derivatives.
# Note: files already tracked by git under these paths will remain tracked until explicitly untracked.
.github/assets/
icons/enhanced/

# Node (resume site tooling)
node_modules/
npm-debug.log
